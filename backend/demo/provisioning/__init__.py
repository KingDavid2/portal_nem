"""Demo workspace provisioning (port of paysys/crm Demo::Profiles).

Each persona provisioner extends ``DemoProvisioner`` to seed a brand-new
real tenant with the data required for its demo scenario.
"""