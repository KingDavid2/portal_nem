import { ChevronDown } from "lucide-react"; import { Card } from "./card"; import { PasoRow } from "./paso-row";
export type PasoItem={number:number|string;content:React.ReactNode}; type Props=React.ComponentProps<typeof Card>&{title:React.ReactNode;subtitle?:React.ReactNode;status?:React.ReactNode;actions?:React.ReactNode;session?:React.ReactNode;pasoRows?:PasoItem[]};
export function MomentoCard({title,subtitle,status,actions,session,pasoRows=[],children,...props}:Props){return <Card className="overflow-hidden border border-foreground/12 p-0" {...props}><header className="flex items-center gap-3 border-b border-foreground/12 px-[18px] py-[14px]"><ChevronDown aria-hidden className="size-[18px] text-foreground/60"/><div className="flex-1"><h3 className="font-semibold">{title}</h3>{subtitle&&<p className="text-sm text-foreground/60">{subtitle}</p>}</div>{status}{actions}</header><div className="p-[18px]">{session&&<div className="mb-3">{session}</div>}<div>{pasoRows.map((row)=><PasoRow key={row.number} number={row.number}>{row.content}</PasoRow>)}</div>{children}</div></Card>;}
