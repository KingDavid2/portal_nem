import { Plus, Star } from "lucide-react"; import { Card } from "./card"; import { PdaRow } from "./pda-row";
export type PdaItem={id:string;content:React.ReactNode;selected?:boolean;featured?:boolean}; type Props=React.ComponentProps<typeof Card>&{title:React.ReactNode;selected?:boolean;actions?:React.ReactNode;pdaRows?:PdaItem[];onSelectedChange?:(id:string,selected:boolean)=>void;onAddPda?:()=>void;readOnly?:boolean};
export function ContenidoCard({title,selected=false,actions,pdaRows=[],onSelectedChange,onAddPda,readOnly=false,children,...props}:Props){return <Card className="overflow-hidden border border-primary" {...props}><header className="flex items-center gap-2.5 bg-primary/10 px-4 py-3">{!readOnly && <input aria-label="Seleccionar contenido" type="checkbox" checked={selected} onChange={()=>{}} className="size-[18px] accent-primary"/>}<Star className="size-4 fill-success text-success"/><h3 className="flex-1 text-sm font-semibold">{title}</h3>{actions}</header><div className="grid gap-2 p-4"><p className="text-xs font-semibold text-foreground/60">Procesos de Desarrollo de Aprendizaje (PDAs):</p><div aria-label="Lista de PDAs">{pdaRows.map((row)=><PdaRow key={row.id} featured={row.featured} selected={row.selected} readOnly={readOnly} onSelectedChange={(value)=>onSelectedChange?.(row.id,value)}>{row.content}</PdaRow>)}</div>{onAddPda&&<button type="button" onClick={onAddPda} className="flex w-fit items-center gap-1.5 rounded-md border border-success px-3.5 py-2 text-sm text-success"><Plus className="size-4"/>Agregar PDA</button>}{children}</div></Card>;}
