import { AlertTriangle, Star } from "lucide-react"; import { cn } from "@/lib/utils";
type Props=React.ComponentProps<"label">&{featured?:boolean;selected?:boolean;onSelectedChange?:(value:boolean)=>void;readOnly?:boolean;unofficial?:boolean};
export function PdaRow({featured,selected=false,onSelectedChange,readOnly=false,unofficial=false,className,children,...props}:Props){const content=unofficial?<><AlertTriangle aria-hidden className="size-4 shrink-0 text-destructive"/><span>{children}</span><span className="sr-only">fuera de tu selección</span></>:<><Star aria-hidden className={cn("size-4 shrink-0",featured?"fill-success text-success":"text-success")}/><span>{children}</span></>;const classes=cn("flex items-center gap-2.5 py-2 text-[13px] leading-relaxed text-foreground/80",className);if(readOnly)return <div className={classes} {...(props as React.ComponentProps<"div">)}>{content}</div>;return <label className={classes} {...props}><input aria-label="Seleccionar PDA" type="checkbox" checked={selected} onChange={(event)=>onSelectedChange?.(event.target.checked)} className="size-[18px] rounded border-foreground/35 accent-primary"/>{content}</label>;}
