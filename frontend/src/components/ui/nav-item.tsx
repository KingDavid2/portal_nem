import Link from "next/link"; import type { LucideIcon } from "lucide-react"; import { cn } from "@/lib/utils";
type NavItemProps = Omit<React.ComponentProps<typeof Link>, "href" | "children"> & { href: string; label: string; icon: LucideIcon; active?: boolean };
export function NavItem({ href, label, icon: Icon, active = false, className, ...props }: NavItemProps) { return <Link href={href} aria-current={active ? "page" : undefined} className={cn("flex items-center gap-3 rounded-full px-4 py-2.5 text-sm font-medium text-muted-foreground hover:bg-sidebar-accent hover:text-foreground", active && "bg-sidebar-accent text-foreground", className)} {...props}><Icon aria-hidden className="size-[22px]" />{label}</Link>; }
