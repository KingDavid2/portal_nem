import { FormField } from "@/components/ui/form-field";
type Item={id:number;label:string}; type Props={schools:Item[];schoolYears:Item[];groups:Item[];schoolId:number|null;schoolYearId:number|null;groupId:number|null;onSchoolChange:(id:number|null)=>void;onSchoolYearChange:(id:number|null)=>void;onGroupChange:(id:number|null)=>void};
export function SchoolContextFilters({schools,schoolYears,groups,schoolId,schoolYearId,groupId,onSchoolChange,onSchoolYearChange,onGroupChange}:Props){const number=(value:string)=>value?Number(value):null;return <div className="grid gap-4 rounded-xl bg-card p-5 shadow-card md:grid-cols-3"><FormField label="Escuela"><select value={schoolId??""} onChange={(e)=>{onSchoolChange(number(e.target.value));onSchoolYearChange(null);onGroupChange(null)}} className="h-10 rounded-md border border-input bg-card px-3 text-sm"><option value="">Selecciona una escuela…</option>{schools.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></FormField><FormField label="Ciclo escolar"><select value={schoolYearId??""} disabled={schoolId===null} onChange={(e)=>{onSchoolYearChange(number(e.target.value));onGroupChange(null)}} className="h-10 rounded-md border border-input bg-card px-3 text-sm"><option value="">Selecciona un ciclo escolar…</option>{schoolYears.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></FormField><FormField label="Grupo"><select value={groupId??""} disabled={schoolYearId===null} onChange={(e)=>onGroupChange(number(e.target.value))} className="h-10 rounded-md border border-input bg-card px-3 text-sm"><option value="">Selecciona un grupo…</option>{groups.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></FormField></div>}
